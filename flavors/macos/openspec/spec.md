# macOS host

### Requirement: Portable macOS execution

The generated application SHALL use only portable host interfaces available on macOS.

#### Scenario: Host execution

- **WHEN** the application is built for macOS
- **THEN** its generated tests and entrypoint run on the selected host
